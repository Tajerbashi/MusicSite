@model IEnumerable<DAL.ViewModels.ViewSinger>
@{
    string Name = "";
}
<ul class="list-group">
    @foreach (var item in Model)
    {
        if (Name != item.SingerName)
        {
            <li class="list-group-item ItemCountry">
                <a href="#">@item.SingerName</a>
            </li>
        }
        Name = item.SingerName;
    }
</ul>

